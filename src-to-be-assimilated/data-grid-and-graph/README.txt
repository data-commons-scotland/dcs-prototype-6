
For developing
	npm install    # probably just the first time 
	The data files are generated by prep-data.ipynb

For running (with the dcs-prototype-6 content)
  npm run-script build
	cd build
	Copy all into ../../../public/data-grid-and-graph/